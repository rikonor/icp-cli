name: Update Brew Repository
description: Update Brew Repository

inputs:
  version:
    description: Version number to distribute
    required: true

runs:
  using: composite
  steps:
    - shell: bash
      run: |
        echo ${{ inputs.version }}
