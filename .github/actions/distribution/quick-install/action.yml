name: Quick Install Action
description: Handles Quick Install script distribution

inputs:
  version:
    description: Version number to distribute
    required: true

runs:
  using: composite
  steps:
    - name: Print Quick Install Version
      shell: bash
      run: |
        echo "Distributing quick-install for version ${{ inputs.version }}"
