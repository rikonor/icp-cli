package icp:cli@0.3.0;

interface cli {
  // spec provides a schema a for the cli
  spec: func() -> string;

  // run the cli portion of the extension
  run: func(args: list<string>) -> u8;
}

interface init {
  init: func() -> result<_, string>;
}

world extension {
  import misc;
  import filesystem;

  export init;
  export cli;
}
