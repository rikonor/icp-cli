package icp:cli@0.2.0; // Bumped version due to adding read-file to filesystem

interface misc {
  print: func(s: string);
  rand: func() -> u8;
  time: func() -> u64;
}

interface cli {
  // spec provides a schema a for the cli
  spec: func() -> string;

  // run the cli portion of the extension
  run: func(args: list<string>) -> u8;
}

world extension {
  import misc;
  import filesystem;

  export cli;
}
