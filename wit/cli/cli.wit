package icp:cli@0.0.1;

interface misc {
  print: func(s: string);
  rand: func() -> u8;
  time: func() -> u64;
}

interface cli {
  // spec provides a schema a for the cli
  spec: func() -> string;

  // run the cli portion of the extension
  run: func(args: list<string>) -> u8;
}

world extension {
  import misc;
  export cli;
}
